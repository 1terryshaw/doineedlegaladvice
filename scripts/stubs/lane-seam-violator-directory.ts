/**
 * RED CONTROL for PS-L3 (the mirror). NOT SHIPPED, NOT IMPORTED BY ANYTHING.
 *
 * The mention below is ACTIVE CODE, not a comment — the matcher strips comments first, so a
 * control written as prose would prove nothing about the matcher that runs in anger.
 */
export const plantedLaneMention = "select 1 from legal_submitted_listing";
